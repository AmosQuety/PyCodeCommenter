# Store every text file with LF line endings in the repository, so diffs
# only show real changes. (The tool itself still keeps each user file's own
# line endings when it rewrites it.)
* text=auto eol=lf
